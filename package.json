{
  "name": "phaser-ts",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cpx 'src/**/*' build/ && tsc && rollup -c",
    "watch": "npm run watch-rollup & npm run watch-tsc & npm run watch-copy & npm run livereload & npm run serve",
    "watch-tsc": "tsc -w ",
    "watch-rollup": "rollup -c -w",
    "serve": "http-server build/ -gs -p 8087 && echo 'Server launched at http://localhost:8087'",
    "livereload": "livereload build/ -w 500",
    "postinstall": "typings install && npm run build"
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "lodash": "^4.16.2"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-shell": "^0.5.2",
    "livereload": "^0.5.0",
    "phaser": "^2.6.2",
    "rollup": "^0.36.1",
    "rollup-plugin-commonjs": "^5.0.4",
    "rollup-plugin-inject": "^2.0.0",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rollup-watch": "^2.5.0",
    "typescript": "^2.0.3",
    "typings": "^1.4.0",
    "watch": "^1.0.0"
  }
}
